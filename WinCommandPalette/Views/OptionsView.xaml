<Window x:Class="WinCommandPalette.Views.OptionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinCommandPalette"
        xmlns:ap="clr-namespace:WinCommandPalette.AttachedProperties"
        mc:Ignorable="d"
        Title="WinCommand Palette Options"
        MinWidth="650" MinHeight="550"
        WindowStartupLocation="CenterScreen" Topmost="True"
        Background="White">


    <Window.Resources>
        <Style x:Key="MenuTextButton" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Calibri"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Margin" Value="0 2 0 2"/>
            <Setter Property="Background" Value="Red"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="190"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Background="#FF2a2a2a" VerticalAlignment="Stretch" LastChildFill="False">

            <TextBlock Text="GENERAL" VerticalAlignment="Center" HorizontalAlignment="Center" DockPanel.Dock="Top"
                    Background="Transparent" Foreground="White"
                    FontSize="12" Height="20" TextTrimming="CharacterEllipsis"/>
        </DockPanel>
    </Grid>
</Window>
